<div class="container">
  <p>Plan Name</p>
  <input type="text" id="plan-name-select" placeholder="e.g. Bible in 365 Days"> 
  <p>Start Book</p>
  <select id="first-book-select">
    <%for (var bookNum in context.books){%>
      <option value="<%=context.books[bookNum]%>"><%=context.books[bookNum]%></option>
    <%}%>
  </select> 
  <p>End Book</p>
  <select id="last-book-select">
    <%for (var bookNum in context.books) {%>
      <option value="<%=context.books[bookNum]%>"><%=context.books[bookNum]%></option>
    <%}%>
  </select>
  <button id="cancel-button">Cancel</button>
  <button id="done-button">Done</button>
  
</div>

<!--
  Need to fix this code to display the profile of the reading plan!!!!

<div class="all_graphs">
  <div class="graph">
    <p>% days complete, days left:</p>
  </div>
  <div class="graph">
    <p>% books complete, days left:</p>
  </div>
  <div class="graph">
    <p>total hours spent each day of the week</p>
  </div>
  <div class="text-center">
    <button id="stats" type="button" class="btn btn-default">Stats</button>
  </div>
</div>
-->


<script src="/scripts/addplan.js"></script>
<link rel="stylesheet" href="/styles/homepage.css"/>